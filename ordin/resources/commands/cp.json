{
  "schema_version": "ordin.command_card.v1",
  "command": "cp",
  "summary": "Copy files and directories.",
  "aliases": ["copy file", "copy directory"],
  "intents": [
    "copy a file",
    "copy a directory",
    "duplicate a file"
  ],
  "default_risk": "medium",
  "risk_tags": ["filesystem_write"],
  "examples": [
    {
      "command": "cp source.txt destination.txt",
      "explanation": "Copy a file to a new path."
    }
  ],
  "templates": [
    {
      "command": "cp {path} <destination>",
      "description": "Copy a file to a destination."
    }
  ]
}
