{
  "schema_version": "ordin.command_card.v1",
  "command": "unzip",
  "summary": "List or extract zip archives.",
  "aliases": ["extract zip", "unpack zip", "list zip"],
  "intents": ["extract zip file", "list zip contents", "unpack archive"],
  "default_risk": "medium",
  "risk_tags": ["filesystem_write", "archive_extract"],
  "examples": [{"command": "unzip -l archive.zip", "explanation": "List zip contents before extracting."}],
  "templates": [
    {"command": "unzip -l {path}", "description": "List zip contents before extracting."},
    {"command": "unzip {path}", "description": "Extract a zip archive."}
  ]
}
