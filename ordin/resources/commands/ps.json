{
  "schema_version": "ordin.command_card.v1",
  "command": "ps",
  "summary": "Report running processes.",
  "aliases": ["process list", "running processes", "processes"],
  "intents": [
    "show running processes",
    "find process by name",
    "inspect process list",
    "see what is running"
  ],
  "default_risk": "low",
  "risk_tags": ["inspection", "process_metadata"],
  "examples": [
    {
      "command": "ps aux",
      "explanation": "Show running processes with details."
    }
  ],
  "templates": [
    {
      "command": "ps aux",
      "description": "Show running processes with details."
    },
    {
      "command": "ps aux | grep {process}",
      "description": "Search running processes by name."
    }
  ]
}
