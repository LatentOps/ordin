{
  "schema_version": "ordin.command_card.v1",
  "command": "ping",
  "summary": "Send ICMP echo probes to test network reachability.",
  "aliases": ["network reachable", "check host", "latency"],
  "intents": ["check if host is reachable", "test network latency", "ping a server"],
  "default_risk": "low",
  "risk_tags": ["network_access", "inspection"],
  "examples": [{"command": "ping -c 4 example.com", "explanation": "Send four ping probes."}],
  "templates": [{"command": "ping -c 4 {host}", "description": "Test whether a host is reachable."}]
}
