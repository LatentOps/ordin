{
  "schema_version": "ordin.command_card.v1",
  "command": "grep",
  "summary": "Search text using patterns.",
  "aliases": ["search text", "find text", "match pattern"],
  "intents": ["search text in files", "find a string", "grep logs", "match lines"],
  "default_risk": "low",
  "risk_tags": ["inspection", "text_search"],
  "examples": [{"command": "grep -R \"TODO\" .", "explanation": "Search recursively for TODO."}],
  "templates": [{"command": "grep -R \"{pattern}\" {path}", "description": "Search text recursively under a path."}]
}
