{
  "schema_version": "ordin.command_card.v1",
  "command": "fuser",
  "summary": "Identify processes using files, sockets, or filesystems.",
  "aliases": ["process using file", "port process"],
  "intents": [
    "find process using a port",
    "find process using a file",
    "stop process on port"
  ],
  "default_risk": "medium",
  "risk_tags": ["process_metadata", "process_kill"],
  "examples": [
    {
      "command": "fuser 3000/tcp",
      "explanation": "Show process IDs using TCP port 3000."
    }
  ],
  "templates": [
    {
      "command": "fuser {port}/tcp",
      "description": "Show processes using a TCP port."
    }
  ]
}
