{
  "schema_version": "ordin.command_card.v1",
  "command": "mkdir",
  "summary": "Create directories.",
  "aliases": ["make directory", "create folder"],
  "intents": [
    "create a directory",
    "make a folder",
    "create nested directories"
  ],
  "default_risk": "medium",
  "risk_tags": ["filesystem_write"],
  "examples": [
    {
      "command": "mkdir -p path/to/folder",
      "explanation": "Create nested directories if needed."
    }
  ],
  "templates": [
    {
      "command": "mkdir -p {path}",
      "description": "Create a directory path."
    }
  ]
}
