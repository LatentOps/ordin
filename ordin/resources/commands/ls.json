{
  "schema_version": "ordin.command_card.v1",
  "command": "ls",
  "summary": "List directory contents.",
  "aliases": ["list files", "show files", "directory listing"],
  "intents": [
    "list files",
    "show hidden files",
    "inspect directory contents",
    "see what is in a folder"
  ],
  "default_risk": "low",
  "risk_tags": ["inspection", "filesystem_metadata"],
  "examples": [
    {
      "command": "ls -la",
      "explanation": "List all files with details."
    }
  ],
  "templates": [
    {
      "command": "ls -la {path}",
      "description": "List files with details at a path."
    }
  ]
}
