{
  "schema_version": "ordin.command_card.v1",
  "command": "df",
  "summary": "Report filesystem disk space usage.",
  "aliases": ["disk free", "free space", "filesystem space"],
  "intents": [
    "show free disk space",
    "check filesystem usage",
    "see disk space",
    "find full disk"
  ],
  "default_risk": "low",
  "risk_tags": ["inspection", "filesystem_metadata"],
  "examples": [
    {
      "command": "df -h",
      "explanation": "Show filesystem space in human-readable units."
    }
  ],
  "templates": [
    {
      "command": "df -h",
      "description": "Show filesystem free and used space."
    }
  ]
}
