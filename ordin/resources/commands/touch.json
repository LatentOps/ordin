{
  "schema_version": "ordin.command_card.v1",
  "command": "touch",
  "summary": "Create files or update timestamps.",
  "aliases": ["create file", "update timestamp"],
  "intents": ["create empty file", "update file timestamp", "make a file"],
  "default_risk": "medium",
  "risk_tags": ["filesystem_write"],
  "examples": [{"command": "touch notes.txt", "explanation": "Create notes.txt if it does not exist."}],
  "templates": [{"command": "touch {path}", "description": "Create a file or update its timestamp."}]
}
